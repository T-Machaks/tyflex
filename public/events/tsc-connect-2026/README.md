# TSC Printronix Connect 2026 — event photos

Drop the event photos here as `01.jpg`, `02.jpg`, … (JPEG or WebP, landscape,
ideally ~1600px wide, each < 400 KB).

Then in `content/blog/tyflex-at-tsc-printronix-connect-2026.mdx` uncomment the
`gallery:` block in the frontmatter and list the files you added. The post
renders them as a grid above the article body.
