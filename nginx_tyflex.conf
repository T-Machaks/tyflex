# Add this location block inside the server {} for projects.tyflex.co.zw
# in /etc/nginx/conf.d/tyflex.conf

location /tyflex/ {
    alias /var/www/tyflex/tyflex/;
    index index.html;
    try_files $uri $uri/ $uri/index.html =404;
}
