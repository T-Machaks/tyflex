node_modules/
.next/
out/
.env
.env.local
.env*.local

# Local-disk fallback for the document portal's file storage (dev only —
# used when S3 isn't configured; see src/lib/portal/storage.ts)
.portal-uploads/

# TypeScript incremental build cache — machine-specific, not deploy input.
tsconfig.tsbuildinfo
