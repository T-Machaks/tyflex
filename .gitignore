node_modules/
.next/
out/
.env
.env.local
.env*.local

# Local-disk fallback for the document portal's file storage (dev only —
# used when S3 isn't configured; see src/lib/portal/storage.ts)
.portal-uploads/

# TypeScript incremental build cache — machine-specific, not deploy input.
tsconfig.tsbuildinfo

# Vendor source material (datasheets/pricelists/pictures) — processed
# assets live in public/; the raw dump is not a deploy input.
/Webstore/

dev3100.*
dev-3100.*
dev-server.log
